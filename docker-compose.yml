services:
  adc-postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5435:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ai_data_cleanser
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
